using Microsoft.AspNetCore.Mvc.Rendering;
//using StatusCenterDataLayer.Models;
//using StatusCenterDataLayer.Repositories;
using System.Diagnostics.Eventing.Reader;

namespace StatusCenter.Services
{
    public class CachedObjects : ICachedObjects
    {
        public CachedObjects(
            //ISignalTypeRepository signalTypeRepository, 
            )
        {
            //SignalTypes = signalTypeRepository.GetAllAsync().Result.ToList();
            //ConfigurationTypes = configurationTypeRepository.GetAllAsync().Result.ToList();
            //SoftwareVersions = softwareVersionRepository.GetAllAsync().Result.ToList();
            //Units = unitInfoRepository.GetAllAsync().Result.ToList();
            //AlarmLevelGroups = alarmLevelGroupRepository.GetAllAsync().Result.ToList();
            //AlarmLevelMaps = alarmLevelMapRepository.GetAllAsync().Result.ToList();
        }

        //public List<SignalType> SignalTypes { get; }
        //public List<ConfigurationType> ConfigurationTypes { get; }
        //public List<SoftwareVersion> SoftwareVersions { get; }
        //public List<UnitInfo> Units { get; }
        //public List<AlarmLevelsGroup> AlarmLevelGroups { get; }
        //public List<AlarmLevelsMap> AlarmLevelMaps { get; }

    }
}
